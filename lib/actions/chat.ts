'use server'

//создание
//удаление

export async function createChat(userId:string){
    try {
        // const session = await auth();
        throw "todo";
    } catch (error) {
        return { error: "failed_request" }
    }
}
